apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
    name: moc-group-sno-du-perfprofile-policy
    namespace: groups-sub
    annotations:
        policy.open-cluster-management.io/categories: CM Configuration Management
        policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
        policy.open-cluster-management.io/standards: NIST SP 800-53
spec:
    remediationAction: enforce
    disabled: false
    policy-templates:
        - objectDefinition:
            apiVersion: policy.open-cluster-management.io/v1
            kind: ConfigurationPolicy
            metadata:
                name: moc-group-sno-du-perfprofile-policy-config
            spec:
                remediationAction: enforce
                severity: low
                object-templates:
                    - complianceType: musthave
                      objectDefinition:
                        apiVersion: v1
                        kind: ConfigMap
                        metadata:
                            name: moc-sno-perfprofile-policy
                            namespace: default
                        data:
                            spec: | 
                                additionalKernelArgs: []
                                cpu:
                                    isolated: '{{hub fromConfigMap "default" (printf "moc-siteconfig-%s" .ManagedClusterName) "performanceProfile-cpu-isolated" hub}}'
                                    reserved: '{{hub fromConfigMap "default" (printf "moc-siteconfig-%s" .ManagedClusterName) "performanceProfile-cpu-reserved" hub}}'
                                hugepages:
                                    defaultHugepagesSize: 1G
                                    pages:
                                        - count: '{{hub fromConfigMap "default" (printf "moc-siteconfig-%s" .ManagedClusterName) "performanceProfile-pages-count" hub}}'
                                          size: '{{hub fromConfigMap "default" (printf "moc-siteconfig-%s" .ManagedClusterName) "performanceProfile-pages-size" | toInt hub}}'
                                machineConfigPoolSelector:
                                    pools.operator.machineconfiguration.openshift.io/master: ""
                                net:
                                    userLevelNetworking: true
                                nodeSelector:
                                    node-role.kubernetes.io/master: ""
                                numa:
                                    topologyPolicy: restricted
                                realTimeKernel:
                                    enabled: true
