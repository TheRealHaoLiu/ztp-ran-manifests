apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
    name: site-sno00037-configmap-policy-placementrule
    namespace: sites-sub
spec:
    clusterSelector:
        matchExpressions:
            - key: name
              operator: In
              values:
                - sno00037
