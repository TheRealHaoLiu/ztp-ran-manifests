apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
    name: moc-site-sno01982-configmap-policy-placementrule
    namespace: sites-sub
spec:
    clusterSelector:
        matchExpressions:
            - key: name
              operator: In
              values:
                - sno01982
